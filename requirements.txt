# 공통 (필수)
requests>=2.31.0
python-dotenv>=1.0.0

# 클라우드 LLM provider — 사용하는 것만 설치하면 됩니다.
# (지연 import 구조라 미설치 provider는 import되지 않습니다)
# LLM_PROVIDER=openai
# openai>=1.0.0
# LLM_PROVIDER=gemini
# google-genai>=1.0.0
# LLM_PROVIDER=claude
# anthropic>=0.40.0
